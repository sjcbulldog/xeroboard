o  tree example, add icons for open/closed folders and leaf nodes
o  multiple applets in a single window support via tabs
o  network tables data via javascript in browser vis electron pipe
o  board applet
